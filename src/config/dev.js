const devConfig = {
    rootDirectory: process.cwd() + "/dist/customer",
    optionFilePath: process.cwd() + "/dist/customer/options.json",
    templatePath: process.cwd() + "/../front/html",
};

module.exports = devConfig;
